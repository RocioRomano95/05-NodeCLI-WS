function split(arr) {
  let mitadDelArr = Math.floor(arr.length / 2);
  let primeraMitad = arr.slice(0, mitadDelArr);
  let segundaMitad = arr.slice(mitadDelArr);

  return [primeraMitad, segundaMitad];
}
function merge(arr){
  let newMerge=[]
  let index1=0
  let index2=0
  while(arr[index1]< arr[index2] || !arr[index2]){
    if(){newMerge.push(arr[index1])
  }else{
    newMerge.push
  }}
}